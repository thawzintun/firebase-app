import React from "react";

const Intro = () => {
    return (
        <section className="intro">
            <h1>Welcome from FireNote.</h1>
            <p>
                Write something in the above field. <br />
                Click 'Add Note' button to create something.
            </p>
        </section>
    );
};

export default Intro;
